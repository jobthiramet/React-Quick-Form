{
  "scripts": {
    "dev": "npm run dev --prefix my-project",
    "build": "npm run build --prefix my-project",
    "lint": "npm run lint --prefix my-project",
    "preview": "npm run preview --prefix my-project"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "tailwindcss": "^4.3.2"
  }
}
